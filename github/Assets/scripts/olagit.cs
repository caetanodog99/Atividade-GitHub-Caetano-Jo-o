using UnityEngine;

public class olagit : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Olá GitHub!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
